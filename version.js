/* Single source of truth for this app's version.
   Both kid and parent screens show it and the service worker names its cache
   after it, so you can always tell what an installed iPad is running.
   Bump with ../bump.sh trawley-coin — never edit the number in two places. */
self.APP_VERSION = "7";
self.APP_DATE = "2026-08-23";
